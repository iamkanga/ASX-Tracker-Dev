{
  "name": "asx-tracker-dev",
  "private": true,
  "version": "0.0.0",
  "description": "ASX Tracker Dev utilities",
  "scripts": {
    "serve:isolation": "node scripts/isolation-serve.js"
  },
  "dependencies": {
    "@playwright/test": "^1.55.0",
    "playwright": "^1.55.0"
  }
}
